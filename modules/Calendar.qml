import QtQuick
import Quickshell.Widgets
import Quickshell
import qs
import qs.components
import qs.widgets
import qs.modules

PanelWindow{
    id: root;
    implicitHeight: 100;
    implicitWidth: 100;
    exclusiveZone: 0;
    Rectangle{
        anchors.fill: parent
    }
}